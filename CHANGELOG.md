# 版本记录

## 未发布

- 应用图标改为相连的双重门廊标记，统一用于侧栏、启动画面、Windows 桌面与任务栏及 Linux 应用菜单；提供多尺寸 PNG、ICO 和 SVG。

- 项目正式更名为“千万间 Roomillion”：应用标题、界面品牌、安装包、Linux 可执行文件、文档和许可证署名统一使用新名称；房间文件改用 `.room`，并继续接受旧版 `.zroom` 导入和双击打开。

- 外观设置新增 6 套真正改变组件语言的风格主题：宣纸丹青、宋瓷雅白、无界白、编辑部、复古终端与霓虹玻璃；其中四套使用浅色侧栏，不再只是更换颜色。

- 将项目导入从 2 MiB/180 文件的内存快照升级为磁盘型只读完整索引，支持游标分页和 64 KiB 文本分页；依赖、构建、数据及疑似秘密目录继续排除，二进制文件保留索引元数据。
- 将 `room-app@1` 升级为真正的多文件自由房间：AI 可分步写入任意数量的安全相对路径模块，JS/MJS 支持静态模块组合，附加 CSS 自动加载，安装和后续 AI 修改完整保留全部源码。
- Agent 输出、上下文和超时改为模型感知并支持 `/runtime` 按任务设置；默认取消任务 Token、请求次数、工具错误和子 Agent 数量配额。
- 新增房间 HTTP 响应流和文件导出流；数据库切换为磁盘 SQLite，向量、大文件、备份和房间业务容量取消旧 P0/P1 演示型上限，仅保留路径、权限、模型真实能力与反压缩炸弹等技术边界。
- 移除房间 AI 接口人为写死的 16–4000 输出 Token 区间：房间现在只需提供正整数，工作台按所选模型目录声明的真实输出上限适配，未声明上限时交由提供商处理。
- 修复非全屏窗口中房间数量较多时的可达性：左侧房间列表独立滚动且底部设置固定可见；主页取消嵌套卡片滚动，超过首屏的房间统一随主页滚动，并显示轻量滚动条。
- AI 创建房间输入框统一为“回车发送、Shift + 回车换行”；斜杠指令菜单改用 Tab 补全，不再截获回车，并避免中文输入法确认候选字时误发送。

## 0.3.0-alpha.39 — 2026-09-12

- 修复 OpenCode Go、MiniMax 等 OpenAI 兼容网关在工具调用回复缺少 `usage` 时，Pi 在下一轮计算上下文读取 `totalTokens` 报错并立即中断的问题；当前流式消息与历史会话恢复都会补齐标准用量结构，未知用量仍明确标为未返回。
- “继续 / 按推荐 / 你决定”现在会采用上一轮问题卡的推荐答案并直接进入方案整理，不再污染需求或重复追问。
- AI 创建房间新增受限子 Agent 编排：实施阶段可将架构分析、代码审查和测试审查拆给最多三个子 Agent，主 Agent继续负责取舍、写入、验证和安装。
- 新增 `/help`、`/status`、`/context`、`/compact`、`/models`、`/model`、`/tools`、`/retry` 常用指令；输入 `/` 会显示可键盘选择的命令菜单。
- Agent 工作时输入框不再锁死；用户可立即发送文字或图片补充要求，Pi 会在当前工具步骤结束后把消息作为 steering 输入接入当前运行。
- 新增持久语义上下文摘要和自动压缩：临近模型上下文上限时自动生成结构化交接摘要，保留界面完整聊天记录，并以本地安全压缩作为失败回退。
- 房间开发提示改为第一版体验优先：低风险细节采用推荐默认，不把技术选型或边界说明推给非程序员；仍保留数据、费用和程序执行所需的必要确认与 Harness 测试。
- 增加缺失用量、旧消息恢复、推荐答案继续、Slash 指令、模型切换、语义压缩和子 Agent 上限的回归测试。

## 0.3.0-alpha.38 — 2026-09-12

- 修复房间 Agent 把实际存在的多文件程序误判为“源码不可读”的问题；当前房间读取接口会递归列出 `program` 内的受控文本文件，并支持分页读取相关模块。
- 新增已安装多文件房间的原位修复 Harness：AI 只能对已读取的 `app/` 文件做唯一精确替换，修改先保存在会话草稿，再对临时副本执行打包、隔离启动和重载检查，通过后才原位升级。
- 修复房间更新时沿用已删除权限的问题；原有授权只保留新清单仍然申请的交集，避免 Harness 自己制造“授予未申请权限”安装失败。
- 压缩已完成的源码写入参数、重复能力结果和模型思考签名，阻止长工具循环把上下文推到模型上限；同一轮重复读取会被明确拦截。
- 实施中断或达到单轮限额后保留已确认方案和草稿；失败原因询问不再被当成新需求，用户可点击“继续实施当前方案”或输入“继续安装”续跑。
- 增加多文件原位修复、权限收敛、上下文压缩、重复调用拦截和中断续跑回归测试；真实 Electron 隔离冒烟覆盖已安装程序模式。

## 0.3.0-alpha.37 — 2026-09-12

- 修复房间 Agent 在澄清阶段无法使用已存在项目快照读取工具的问题；导入项目后可先检查目录并分页读取入口、配置和核心源码，再进行迁移评估。
- 新增当前房间程序的受控只读接口；AI 修改房间时必须先读取现有 HTML、CSS、JavaScript 或声明式定义，读取范围不包含房间数据库、用户业务数据和任意磁盘路径。
- 房间方案新增 AI 使用声明。需要主工作台 AI 的房间会在确认方案时询问是否允许真实调用测试，并可从已配置模型中单独选择测试模型。
- 自由房间测试继续强制执行静态安全、需求契约和隔离启动/重载；获得授权时额外执行小 Token 真实 AI 调用，失败阻止安装，未授权绝不自动消耗 Token。
- 增加受控房间读取、路径隔离、AI 测试授权和指定模型路由的自动回归测试。

## 0.3.0-alpha.36 — 2026-09-12

- 重建同版本 Windows x64 与 generic Linux x64 发行物；Linux 提供解压目录、`tar.xz` 和 AppImage。
- 修复 Linux 缓存恢复后 Electron 启动标记缺失、构建资源未按目标平台重建，以及文件关联测试错误使用 Windows 盘符的问题。
- Linux 窗口管理器允许拒绝抢焦点；独立窗口置顶的严格回归断言继续在 Windows 执行，Linux 验证窗口创建、内容和状态保持。
- Linux 目录版与 AppImage 均在 WSL2 普通用户、空 PATH、零出站网络、SwiftShader 模式下通过打包后冒烟；目标 UOS 的 GPU 和桌面交互实机认证仍待执行。

## 0.3.0-alpha.35 — 2026-09-12

- 新增不要求管理员权限的 Windows 当前用户安装版，注册 `.room` 文件关联；双击房间包会唤醒工作台并进入完整导入预检。
- 新增普通启动单实例转发，重复双击房间不会打开多个工作台；冒烟与隔离运行检查不受单实例锁影响。
- 房间 Manifest 支持内带图标；工作台在主页、侧栏、标签、成果卡与导入预检中显示，旧房间继续回退为名称首字。
- AI 新建房间默认生成离线图标；用户明确指定时，可将最近上传的 2 MiB 以内 PNG/JPEG/WebP 图片作为房间图标打包。
- 增加图标类型、内容、完整性与 SVG 活动内容检查，并增加安装配置、命令行入口、打包和实际渲染回归测试。

## 0.3.0-alpha.34 — 2026-09-12

- 修复 AI 能力中心搜索提供商时输入一个字符就丢失焦点的问题；筛选保留搜索框焦点和光标。
- 搜索框支持按下方向键进入结果列表；中文输入法组词期间不触发焦点切换。
- 增加 Electron 界面回归检查，覆盖连续输入、光标中间编辑、删除、无结果、粘贴、模拟中文组词及键盘选择。

## 0.3.0-alpha.33 — 2026-09-12

- 修复模型凭据状态误导，按模型显示状态；缺密钥时阻止发送并提示配置。
- 修复带数据导入的冲突提示，明确替换而非合并，并提供数据恢复检查点入口。
- 修复 hidden 元素被组件样式重新显示的问题；菜单后方显示房间占位，不再露出主页。
- 内置示例显示已安装和可更新版本；已有房间通过预检确认后更新，不直接覆盖。
- 澄清问卷支持单选、多选、文本；补充说明可折叠，修复选项布局和确认按钮样式。
- 单文件启动增加解压等待画面；设置明确数据目录和免安装/数据便携的区别。
- 补充安全政策、贡献指南、基础 CI、问题模板及发布检查清单。

许可证剩余材料、签名、干净机/跨平台实机与远程仓库设置不因本版本自动视为完成。

## 0.3.0-alpha.32

- 房间原生文件对话框跟随当前独立窗口，避免导出时激活主窗口。
- Windows Computer Use 验收报告见 docs/45；历史版本详见 docs 中各阶段记录。
